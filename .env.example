# API
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/postgres
STORAGE_DIR=./data/sources
BUILDER_IMAGE=soroban-verify-builder:local

# Soroban RPC overrides (optional; defaults are set in api/app/config.py)
# RPC_URL_TESTNET=https://soroban-testnet.stellar.org
# RPC_URL_MAINNET=https://soroban-rpc.mainnet.stellar.gateway.fm
# RPC_URL_FUTURENET=https://rpc-futurenet.stellar.org

# PGlite (when running demo/pglite locally)
PGDATA=./data/pglite
PGHOST=0.0.0.0
PGPORT=5432
